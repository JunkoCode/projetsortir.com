{% extends 'base.html.twig' %}

{% block title %}Vérifier votre boite mail!{% endblock %}

{% block body %}
    <div class="container">

        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="alert alert-secondary">

                    <div class="col-12">

                        <p>
                            Si votre Email corespond à un compte, alors un email contenant un lien vous permettra de réinitialiser votre mot de passe.
                            Ce lien est valable {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                        </p>
                        <p>Si vous n'avez rien reçu merci de vérifier dans les emails indésirables ou <a href="{{ path('app_forgot_password_request') }}">Réessayer</a>.</p>


                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}
